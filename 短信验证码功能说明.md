# 短信验证码访问控制功能说明

## 功能概述

为**财务管理**和**账单管理**页面添加了短信验证码访问控制功能。用户在访问这些敏感页面之前，需要通过短信验证码验证身份。

## 实现的功能

### 1. 受保护的页面

以下页面已添加短信验证码验证：

- **财务管理页面**
  - 网页版：`/finance/manage`
  - 移动版：`/finance/mobile-manage`

- **账单管理页面**
  - 网页版：`/bill`
  - 移动版：`/bill/mobile-list`

### 2. 验证流程

1. 用户访问受保护页面时，系统检查 `sessionStorage` 中的验证状态
2. 如果未验证，自动弹出短信验证码模态框
3. 用户输入手机号并获取验证码
4. 验证码校验通过后，页面内容才会显示
5. 验证状态保存在 `sessionStorage` 中，页面刷新后仍然有效
6. 当用户离开页面（路由变化）时，验证状态自动清除

### 3. 核心组件

#### 3.1 SmsVerificationModal 组件

位置：`playground/src/components/SmsVerificationModal.vue`

功能：

- 手机号输入和验证
- 发送短信验证码（带倒计时）
- 验证码输入
- 验证码校验

#### 3.2 后端 API

**发送页面访问验证码**

- 接口：`POST /auth/send-page-access-code`
- 参数：`{ phoneNumber: string }`
- 返回：`{ expiresIn: number, debugCode?: string }`

**验证页面访问验证码**

- 接口：`POST /auth/verify-page-access-code`
- 参数：`{ code: string, phoneNumber: string }`
- 返回：`{ verified: true, phoneNumber: string }`

### 4. 技术实现细节

#### 4.1 验证状态管理

```typescript
// 财务管理页面
sessionStorage.setItem('finance-verified', 'true');

// 账单管理页面
sessionStorage.setItem('bill-verified', 'true');
```

#### 4.2 路由守卫

在 `playground/src/router/guard.ts` 中添加了路由守卫配置，识别需要短信验证的路由：

```typescript
const smsVerificationRequiredRoutes = [
  'FinanceManage',
  'FinanceMobileManage',
  'Bill',
  'BillMobileList',
];
```

#### 4.3 页面验证逻辑

每个受保护页面都包含：

- 验证状态检查（`isVerified`）
- 验证模态框引用
- 验证成功回调函数
- 路由变化监听（清除验证状态）

## 使用方法

### 开发环境测试

1. **启动项目**

   ```bash
   pnpm dev:play
   ```

2. **访问财务管理页面**
   - 导航到：财务管理 > 财务管理
   - 系统会自动弹出短信验证码模态框

3. **输入手机号**
   - 输入已注册的 11 位手机号
   - 点击"获取验证码"按钮

4. **获取验证码**
   - 开发环境下，响应中会包含 `debugCode` 字段
   - 可以通过控制台查看或使用实际短信

5. **输入验证码**
   - 输入 6 位验证码
   - 点击提交

6. **访问页面**
   - 验证通过后，页面内容自动加载
   - 可以进行正常的业务操作

### 生产环境配置

确保后端已配置短信服务：

1. **环境变量配置**（`.env` 文件）

```env
# 联麓短信配置
SMS_APP_ID=your_app_id
SMS_MCH_ID=your_mch_id
SMS_SECRET_KEY=your_secret_key
SMS_TEMPLATE_ID=your_template_id

# 验证码配置
LOGIN_SMS_CODE_LENGTH=6
LOGIN_SMS_CODE_TTL=300
LOGIN_SMS_RESEND_INTERVAL=60
LOGIN_SMS_MAX_VERIFY_ATTEMPTS=5
```

2. **短信模板**
   - 需要在联麓短信平台配置相应的短信模板
   - 默认模板 ID：`70344760`
   - 模板内容示例：`您的验证码是：{1}`

## 安全特性

1. **验证码有效期**：5 分钟（300 秒）
2. **重发间隔**：60 秒
3. **最大尝试次数**：5 次
4. **验证码长度**：6 位数字
5. **会话验证**：使用 `sessionStorage` 存储验证状态，页面关闭后自动清除
6. **登录验证**：只有已登录用户才能请求发送验证码

## 文件清单

### 新增文件

1. `playground/src/components/SmsVerificationModal.vue` - 短信验证模态框组件
2. `playground/src/api/page-access.ts` - 页面访问验证 API
3. `apps/backend-mock/api/auth/send-page-access-code.post.ts` - 发送验证码接口
4. `apps/backend-mock/api/auth/verify-page-access-code.post.ts` - 验证验证码接口

### 修改文件

1. `playground/src/views/finance/manage/list.vue` - 财务管理网页版
2. `playground/src/views/finance/manage/mobile-list.vue` - 财务管理移动版
3. `playground/src/views/bill/amount/list.vue` - 账单管理网页版
4. `playground/src/views/bill/amount/mobile-list.vue` - 账单管理移动版
5. `playground/src/router/guard.ts` - 路由守卫配置

## 扩展建议

### 1. 添加更多受保护页面

在 `guard.ts` 中添加路由名称到 `smsVerificationRequiredRoutes` 数组：

```typescript
const smsVerificationRequiredRoutes = [
  'FinanceManage',
  'FinanceMobileManage',
  'Bill',
  'BillMobileList',
  'YourNewPage', // 添加新页面
];
```

并在对应页面组件中添加验证逻辑。

### 2. 自定义验证策略

可以根据业务需求调整验证策略：

- **延长验证有效期**：修改 `sessionStorage` 的 key 命名规则
- **跨页面共享验证状态**：使用统一的验证标识
- **添加验证级别**：区分不同敏感程度的页面

### 3. 增强安全性

- 添加 IP 限制
- 添加设备指纹验证
- 实现验证码图片校验（防止机器人）
- 添加操作日志记录

## 常见问题

### Q1: 验证码发送失败？

- 检查后端短信服务配置
- 确认手机号格式正确（11 位数字）
- 查看浏览器控制台错误信息

### Q2: 验证后页面仍然不显示？

- 检查 `sessionStorage` 是否正确设置
- 确认 `isVerified` 状态是否为 `true`
- 查看页面是否有 JavaScript 错误

### Q3: 如何跳过验证（开发环境）？

可以在开发环境中临时注释掉验证逻辑，但不建议在生产环境中这样做。

### Q4: 验证状态何时清除？

- 页面关闭（`sessionStorage` 自动清除）
- 路由变化到非受保护页面
- 手动清除 `sessionStorage` 中的验证标识

## 维护说明

1. **定期检查短信服务状态**
2. **监控验证码发送频率**
3. **分析验证失败原因**
4. **更新短信模板（如需要）**

## 更新日志

- **2026-03-02**: 初始版本发布
  - 实现财务管理和账单管理页面的短信验证
  - 支持网页版和移动版
  - 集成联麓短信服务
  - 添加路由守卫支持

---

**注意**：此功能涉及用户敏感操作，请确保在生产环境中正确配置短信服务，并遵守相关数据保护法规。
